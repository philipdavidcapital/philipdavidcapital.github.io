# The preview build is a throwaway copy of the site, re-linked to sit flat for
# hosts that serve files rather than directories. It was briefly committed and
# therefore served at /build/preview/ -- a second, duplicate copy of every
# page, live and indexable. It is generated by src/build_preview.py whenever
# it is needed and does not belong in the repository.
build/
